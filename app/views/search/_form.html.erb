<%= search_form_for(@query, url: search_path, method: :get, class:"d-flex rounded") do |f|%>
  <%= f.search_field :title_or_content_or_tags_tag_name_or_user_name_i_cont_any,
    placeholder: "Search...",
    class:"form-control rounded me-2"
  %>
  <%= f.submit "Search!", class:"btn btn-outline-success" %>
<% end %>
